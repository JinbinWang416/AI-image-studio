# 本地使用

- 地址：http://127.0.0.1:8000
- 启动：双击本目录的 `启动图片生成智能体.vbs`，服务在后台运行，浏览器自动打开。重复启动会打开已有服务。
- 停止：先在网页停止正在执行的生成任务，再双击 `停止图片生成智能体.vbs`。
- 关闭浏览器不会停止服务；电脑重启后需重新双击启动文件。
- 在右上角「设置」选择服务商并填写对应 API Key。针对房屋中介的高质量验证，可选择「OpenAI GPT Image」，再使用门店 `01` 卡片上的「生成 6 张新图」。
- 图片目录：本目录的 `output`；网页设置可调整保存位置。
- 后台服务日志：`logs/web_*.stdout.log`、`logs/web_*.stderr.log`。
- 仅监听本机 `127.0.0.1`。

## Windows 桌面安装包

可直接运行项目根目录的 `门店贴纸图片生成智能体桌面安装包_v2.0_20260919.exe`。

安装程序会在当前用户的 `%LOCALAPPDATA%\门店贴纸图片生成智能体` 写入桌面应用，并创建两个桌面快捷方式：

- **门店贴纸图片生成智能体**：启动应用并打开浏览器页面；重复点击只会打开已有页面。
- **停止门店贴纸图片生成智能体**：安全关闭本机服务。

API Key、生成图片、历史版本和日志保存在上述 LocalAppData 目录中，升级安装程序不会覆盖它们。安装包不含 API Key、FLUX 权重或现有 `output` 图片。

在“设置 → 文件与保存”可以上传手机拍摄的门店玻璃照片。该文件保存在输出根目录
的 `_effect_backgrounds`，只由本机效果图合成器读取，不会发送到千问、OpenAI、Gemini
或 Seedream。未选择实拍照片时，首页会把效果图明确标为“模拟门店背景”。

需要从源码重新构建时，执行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tools\build_desktop_installer.ps1
```

构建脚本会生成主应用、单文件安装程序和 SHA-256 校验文件；应用与安装程序共用 `desktop\assets\store-sticker-ai-icon.ico` 图标。

## 全量任务重新生成与批次目录

全量 138 张任务完成后，首页的“开始/继续当前批次”会变为“当前批次已完成”，不再产生全部跳过的空运行。此时点击 **重新生成（新批次）**，程序会在输出根目录下创建类似 `batch_20260919_153000_qwen_qwen-image-3.0` 的独立目录，再开始新的 138 张任务。

旧 `output` 图片保持不变。新批次包含各门店目录、manifest、运行记录和 `_batch.json`；每张图还会带入一组稳定的构图变体指令，以提升相邻批次的视觉差异。中途点击“停止”后，可选择继续当前批次补齐缺图，或点击“重新生成（新批次）”启动另一套目录。

## 阿里云余额不足后的恢复

当阿里云百炼返回 `Arrearage`、`overdue-payment` 或免费额度耗尽时，程序会立刻暂停派发后续图片，不会把未开始的任务写成永久失败。已生成 PNG、当前批次目录和清单都会保留。

充值或恢复按量付费后，按以下顺序操作：

1. 在首页点击 **设置**，在「阿里云百炼」卡片点击 **测试连接**。这一步只验证 API Key 和网络，不会产生图片。
2. 点击 **验证图片出图权限**。它会明确发起并丢弃 1 张测试图，可能产生一次模型费用；只有它成功，才表示图像模型的扣费账户已恢复。
3. 验证成功后，返回首页点击 **开始/继续当前批次**。
4. 程序只补未完成、失败或暂停的图片；不会覆盖已成功的图片，也不会创建新批次目录。

在图片出图权限验证成功前，首页会禁用继续和新批次按钮，并显示恢复提示，避免再次发送无效请求。保存设置不会清除掩码回传的 API Key，设置窗口也会保持打开，以便查看测试结果。

若验证仍返回 `Arrearage`，而费用与成本页已显示可用额度，请先等待阿里云账务同步，再用**同一主账号**在百炼控制台新建通用 API Key、保存后重新验证。新 Key 仍失败时，应用会显示阿里云的 `Request ID`；请携带它向阿里云提交工单。

## FLUX.2 klein 4B 本地样图验证

此路线只用于离线质量验证，**不参与默认 138 张量产**。

1. 运行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\setup_flux_local.ps1`。它会把官方源码、独立虚拟环境和权重安装到 `E:\AIModels\flux2-klein-4b`，并生成包含版本、提交号、文件大小和 SHA-256 的 `model_manifest.json`。
2. 双击 `启动FLUX本地验证.vbs`，或运行 `tools\start_flux_local.ps1`。本地推理服务只绑定 `127.0.0.1:8189`；首次启动会常驻加载模型。
3. 打开网页的「设置」，在 `FLUX.2 klein 4B（本地验证）`卡片点击「测试本地服务」。模型就绪后，在同页的「FLUX 本地样图验证」点击运行。
4. 验证固定为：门店 `01`、V8 提示词、6 张、单并发、1024×1024。结果只写入 `output_local_validation`，原 `output` 不会被改写。
5. 检查 `output_local_validation\_local_validation_report.json` 和 `_人工评分表.csv`。前者记录 PNG SHA-256、种子、耗时、峰值显存和边缘纯白校验；后者用于人工填写文字、主体、构图与可制作性评分。

推理服务禁用提示词联网扩写、图生图和任何远程回退；它只接受正向提示词，忽略负向提示词。停止服务请使用 `停止FLUX本地验证.vbs` 或 `tools\stop_flux_local.ps1`。

## OpenAI 房屋中介 6 图再生成

这条流程只处理门店 `01` 的 6 个既有主题，不会启动其余 132 张任务。

1. 打开网页「设置」，在 **OpenAI GPT Image** 卡片填写 OpenAI 平台 API Key，模型保持 `gpt-image-2.5-flare`，点击「测试连接」后保存，并选中该卡片。
2. 可选填写 **DeepSeek 提示词优化** 的 API Key。默认开启：每张图片提交给 OpenAI 前，DeepSeek 会优化中文构图与商业贴纸层级；它不能更改主标题和副标题。DeepSeek 不可用时会自动回退到 V8 原提示词。
3. 回到首页，在「01 房屋中介门店」点击 **生成 6 张新图**。该任务固定为 1024×1024、`quality=max`、PNG、不透明背景、单并发。
4. 每次运行轮换 6 组构图变体。成图与上一版的感知哈希过于接近时，会换用另一组变体额外重试一次。
5. 当前 6 张成品会被新图替换，但旧文件先归档到 `output\01_房屋中介门店\_history`。清单中保存运行编号、构图指令、实际提示词、模型、耗时与感知哈希；网页图片操作中的「历史」可对照并恢复旧版。

点击网页「停止」会取消正在等待的 DeepSeek 或 OpenAI 请求，未开始的主题不会再发送，取消中的图片不会覆盖当前版本。

## V9 专业贴纸生成

当本地样图确认需要彩色商业贴纸效果时，在网页「设置」的 **V9 专业贴纸生成** 区块运行固定试验。它为门店 `01` 的每个主题生成 3 张**无字**彩色底图（共 18 次推理），按色彩、清晰度、构图覆盖率和视觉平衡自动选优，再用本机微软雅黑精确叠加主标题与副标题。

- 视觉目标：粗描边异形轮廓、分层色块、立体大标题、丝带动线和行业主物件，接近门店玻璃贴成品的视觉密度。
- 输出目录：`output_local_professional_v9`，候选保留在 `candidates`，每主题选中的可用成品在 `selected`；不改写 `output` 或 V8 验证目录。
- 结果清单：`output_local_professional_v9\_professional_validation_report.json`，保存全部候选的种子、评分、耗时、显存和 SHA-256。
- 中文由程序排版而非扩散模型生成，保证文字准确；模型只负责彩色主体和背景。

## 运行环境

使用独立的 Python 3.12 虚拟环境 `.venv`，已安装项目依赖及图片后处理所需的 Pillow。
实际安装版本保存在 `requirements-local.lock.txt`。

在 PowerShell 中进入本目录后，可执行：

```powershell
.\.venv\Scripts\python.exe main.py check
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\local-server.ps1 -NoBrowser
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\local-server.ps1 -Stop -NoBrowser
```

如果迁移目录或重建环境：

```powershell
py -3.12 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements-local.lock.txt
```

## 当前版本的范围

已确认源码提供「本地模拟」、阿里云百炼、OpenAI GPT Image 与本地 FLUX 的生成适配器。界面还列出了可灵、智谱及自定义接口，但压缩包未包含这些服务商的完整生成适配器。
本次部署验证不调用付费生图接口，云端模型的额度与真实出图效果未作验证。

## 部署验证（2026-09-18）

- 数据包校验通过：23 套门店 / 138 条提示词。
- 隔离测试环境中的原项目 Web 测试：59 项通过，0 项失败。
- 隔离目录中完成 6 张 1024×1024 PNG 模拟生成。
- 启动、停止、重复启动检查通过，服务已重新启动并保持运行。
- 浏览器已确认首页和原有图片正常显示。
- 原有配置、API Key 和输出图片保持不变。
- 详细日志：`logs/deployment-check_20260918_212524/`。
