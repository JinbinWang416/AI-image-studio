# -*- coding: utf-8 -*-
"""安全子模块。

划分：
    permissions.py  权限编码的**唯一定义源**（前端导航、角色配置、接口校验、测试都从它生成）
    users.py        用户模型与存储（稳定 ID、登录名规范化、状态机）
    auth.py         密码哈希、会话管理、MFA
    store.py        JSON 持久化（原子写）
    audit.py        审计日志（追加写、不可改）
    url_guard.py    外部 URL 校验（防 SSRF）

设计原则（依据 `平台安全开发提示词.docx`）：
    §2.3  复用成熟组件，不自行设计密码学算法
    §2.4  所有权限由**服务端**校验，不靠隐藏菜单
    §2.5  默认拒绝，最小权限
    §3.14 服务账号独立管理
    §8    审计日志访问受限、防篡改
"""
