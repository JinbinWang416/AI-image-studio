# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build_venv312/

# ---- 配置（含 API Key，绝不可提交）----
# ⚠️ 必须用通配符！只写 `config/settings.json`（精确匹配）挡不住
#    `.bak-*` / `.polluted-*` 变体。生产版仓库就是这样把真实 qwen Key
#    泄到 GitHub 的（初始提交 07df576 里的 settings.json.bak-printreset，
#    117 字符真 Key，2026-09-25 发现后改写历史 + force push 才清除）。
#    本目录 config/ 下同样躺着 6 个含真 Key 的备份副本，靠这行通配挡住。
.env
config/settings.json*
!config/settings.example.json

# ---- 安全：账号 / 会话 / 备份密钥（含密码哈希，绝不可提交）----
data/security/
*.snapshots

# ---- 架构重构过程备份（Phase 0 的 .orig、Phase 1 的迁移前副本）----
_backup_phase0/
_backup_phase1/

# ---- 输出数据与测试产物 ----
output/
output_local_validation/
output_local_professional_v9/
output_backup_*/
backups/
logs/
generated/

# ---- 调研原始资料（体积大，按需保留）----
research/raw/

# ---- 编辑器 / 系统 ----
.vscode/
.idea/
.DS_Store
Thumbs.db

# ---- 桌面版构建产物 ----
desktop/
*.spec
门店贴纸图片生成智能体桌面安装包_*.exe
门店贴纸图片生成智能体桌面安装包_*.exe.sha256
